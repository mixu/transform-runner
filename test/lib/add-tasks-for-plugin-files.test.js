
describe('add tasks for AMD plugin files', function() {

  it('merely applies detective to files that are not plugins', function() {

  });

  it('executes the plugin when .load returns true', function() {

  });

  it('skips the plugin when .load returns false', function() {

  });

});
