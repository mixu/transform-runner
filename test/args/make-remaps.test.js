var assert = require('assert-diff'),
    fixture = require('file-fixture');

describe('make remap expressions', function() {

  it('matches ignores to remaps to a fixed file');

  it('matches remaps to their defined target file');

});
