var assert = require('assert-diff'),
    fixture = require('file-fixture');

describe('is main', function() {

  it('works on a single file main', function() {

  });

  describe('wildcard main', function() {

    it('matches deep files', function() {

    });

    it('does not match files in node_modules subdirectories', function() {

    });

    it('works even if the included target is in a node_modules directory', function() {

    });

  });


});
