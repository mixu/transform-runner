
describe('amd detective', function() {

  it('can resolve dependencies even in the dumb amd format', function() {


  });

  it('passes through plugin!foo paths', function() {

  });

  it('applies the shim.deps parameter from an array', function() {

  });

  it('applies the shim.deps parameter from an object', function() {

  });

  it('resolves when vendor is set but shim is not', function() {

  });

  it('uses the current filepath to resolve', function() {

  });

});
