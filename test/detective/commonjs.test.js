
describe('commonjs detective', function() {

  it('parses dependencies from the given file', function() {


  });

  it('resolves targets relative to the file location', function() {


  });


  it('applies remap glob expressions on the target paths', function() {

  });

  it('can distinguish between two modules of the same name if they resolve to different files', function() {
    // e.g. submodule also uses underscore, but a different version

  });


});
