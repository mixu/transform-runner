// given include paths as absolute globs,
// returns a function (filename) that returns true for main paths
// - any file that has no  node modules folder in the upwards
// path until one of the includes is a hit

module.exports = function(includes) {

  return function(filepath) {

  };
};
